# Lifecycle Hooks

Lifecycle hooks let you execute code at specific points in an element lifecycle, such as creation, rendering, and updating. The full list is documented in the [API](../api/toc.md).
